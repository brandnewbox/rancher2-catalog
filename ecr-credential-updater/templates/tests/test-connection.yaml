apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ecr-credential-updater.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "ecr-credential-updater.name" . }}
    helm.sh/chart: {{ include "ecr-credential-updater.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "ecr-credential-updater.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
